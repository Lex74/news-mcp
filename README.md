# NewsAPI MCP Server

MCP (Model Context Protocol) сервер для получения новостей через NewsAPI. Позволяет LLM запрашивать новости на сегодня по различным темам.

## Возможности

- 🔍 Поиск новостей на сегодня по ключевым словам
- 🌍 Поддержка различных языков
- 📊 Сортировка по релевантности, популярности или дате публикации
- 📰 Получение заголовков, описаний, ссылок и другой информации о статьях

## Быстрый старт

### Виртуальное окружение уже создано!

Виртуальное окружение `venv` уже создано и зависимости установлены. Это изолирует зависимости проекта от других установленных пакетов.

**Активация виртуального окружения:**

```bash
# На Linux/macOS:
source venv/bin/activate
# или используйте скрипт:
source activate.sh

# На Windows:
venv\Scripts\activate
```

**Если нужно переустановить зависимости:**
```bash
source venv/bin/activate
pip install -r requirements.txt
```

**Примечание:** Если у вас конфликт зависимостей с `python-telegram-bot` (требует `httpx~=0.25.2`, а `mcp` требует `httpx>=0.27.1`), используйте виртуальное окружение или обновите `python-telegram-bot`:
```bash
pip install --upgrade python-telegram-bot
```

2. Получите бесплатный API ключ на [NewsAPI.org](https://newsapi.org/) (регистрация занимает минуту)

3. Создайте файл `.env` в корне проекта:
```bash
echo "NEWS_API_KEY=your_api_key_here" > .env
```
Или установите переменную окружения:
```bash
export NEWS_API_KEY=your_api_key_here
```

4. Настройте MCP клиент (см. раздел "Настройка в Cursor/Claude Desktop" ниже)

5. Готово! Теперь LLM может запрашивать новости на сегодня

## Использование

### Запуск сервера

```bash
python server.py
```

### Настройка в Cursor/Claude Desktop

Добавьте сервер в конфигурацию MCP. 

**Для Cursor:**
Конфигурационный файл обычно находится в `~/.cursor/mcp.json` или в настройках Cursor.

**Для Claude Desktop:**
Конфигурационный файл находится в:
- macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`
- Windows: `%APPDATA%\Claude\claude_desktop_config.json`
- Linux: `~/.config/Claude/claude_desktop_config.json`

Пример конфигурации (замените путь на актуальный):

**Важно:** Используйте Python из виртуального окружения `venv`:

```json
{
  "mcpServers": {
    "news-api": {
      "command": "/абсолютный/путь/к/news-mcp/venv/bin/python",
      "args": ["/абсолютный/путь/к/news-mcp/server.py"],
      "env": {
        "NEWS_API_KEY": "your_api_key_here"
      }
    }
  }
}
```

**Для Windows:**
```json
{
  "mcpServers": {
    "news-api": {
      "command": "C:\\путь\\к\\news-mcp\\venv\\Scripts\\python.exe",
      "args": ["C:\\путь\\к\\news-mcp\\server.py"],
      "env": {
        "NEWS_API_KEY": "your_api_key_here"
      }
    }
  }
}
```

**Примечание:** После изменения конфигурации перезапустите Cursor/Claude Desktop.

### Доступные инструменты

#### `get_today_news`

Получить новости на сегодня по заданному запросу.

**Параметры:**
- `query` (обязательный) - Ключевые слова или тема для поиска (например: "bitcoin", "technology", "politics")
- `language` (опциональный) - Язык новостей (ru, en, de, es, fr, it, pt и др.)
- `sort_by` (опциональный) - Сортировка: "relevancy", "popularity", "publishedAt" (по умолчанию: "publishedAt")
- `page_size` (опциональный) - Количество результатов (1-100, по умолчанию: 10)

**Примеры использования:**

```
Получи новости о технологиях на сегодня
Получи новости о политике на русском языке
Найди новости о криптовалюте, отсортированные по популярности
```

## API NewsAPI

Этот сервер использует [NewsAPI Everything endpoint](https://newsapi.org/docs/endpoints/everything) для поиска новостей.

**Важно:** 
- Бесплатный план NewsAPI имеет ограничения (100 запросов в день)
- Для использования в продакшене может потребоваться платный план

## Решение проблем

### Конфликт зависимостей с python-telegram-bot

Если вы получаете ошибку о конфликте `httpx` версий:
- `mcp` требует `httpx>=0.27.1`
- `python-telegram-bot 20.7` требует `httpx~=0.25.2`

**Решения:**

1. **Используйте виртуальное окружение** (рекомендуется):
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # Linux/macOS
   pip install -r requirements.txt
   ```

2. **Обновите python-telegram-bot**:
   ```bash
   pip install --upgrade python-telegram-bot
   ```
   Более новые версии могут поддерживать `httpx>=0.27.1`

3. **Если используете виртуальное окружение в MCP**, обновите конфигурацию:
   ```json
   {
     "mcpServers": {
       "news-api": {
         "command": "/путь/к/venv/bin/python",
         "args": ["/путь/к/news-mcp/server.py"],
         "env": {
           "NEWS_API_KEY": "your_api_key_here"
         }
       }
     }
   }
   ```

## Лицензия

MIT

